<%= form_for(@entry_image) do |f| %>
  <% if @entry_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry_image.errors.count, "error") %> prohibited this entry_image from being saved:</h2>

      <ul>
      <% @entry_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry_id %><br>
    <%= f.text_field :entry_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
